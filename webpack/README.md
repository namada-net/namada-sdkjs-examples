# Namada SDK - Webpack

## Usage

This example instantiates the Namada JS Sdk using Webpack. Update the values in `./src/config/consts.ts`
with valid test accounts (with balance) to see the different calls working.

```bash
npm install

npm run dev
```
